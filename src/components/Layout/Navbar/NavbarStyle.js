import styled from 'styled-components';

import boxBackgroundColor from '../../../shared/theme';

const NavbarContainer = styled.div`
  height: 80px;
  width: 100%;
  background-color: ${boxBackgroundColor};
`

export default NavbarContainer;